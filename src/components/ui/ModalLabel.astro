---
/**
 * Display wrapper: cds-modal-label.
 */
type Props = {
	id?: string;
	class?: string;
	[key: string]: unknown;
};

const { id, class: className, ...rest } = Astro.props;
---

<cds-modal-label id={id} class={className} {...rest}>
	<slot />
</cds-modal-label>
