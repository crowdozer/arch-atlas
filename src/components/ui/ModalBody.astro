---
/**
 * Display wrapper: cds-modal-body.
 */
type Props = {
	id?: string;
	class?: string;
	[key: string]: unknown;
};

const { id, class: className, ...rest } = Astro.props;
---

<cds-modal-body id={id} class={className} {...rest}>
	<slot />
</cds-modal-body>
