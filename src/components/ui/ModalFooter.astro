---
/**
 * Display wrapper: cds-modal-footer.
 */
type Props = {
	id?: string;
	class?: string;
	[key: string]: unknown;
};

const { id, class: className, ...rest } = Astro.props;
---

<cds-modal-footer id={id} class={className} {...rest}>
	<slot />
</cds-modal-footer>
