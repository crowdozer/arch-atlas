# demo-python-app

Small layered Python toy for Arch Atlas **Estimate** demos (static import graph only).
Not a runnable service - import topology for the alluvial UI.

```text
app/main.py          entry
app/api/routes.py    HTTP surface
app/services/*       domain services
app/models/*         data models
app/lib/*            shared helpers
```
