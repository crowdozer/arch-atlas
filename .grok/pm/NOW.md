# Now

**Updated:** 2026-08-01 - PM/NOW orientation floor seed (README Status + catalog)

## Shipped

- Level-1 MVP vertical slice: ZIP/demo open → map catalog → file-hub alluvial; Sentinel zinc/teal shell (`none` - pre-catalog MVP)
- Alluvial engine correctness triage (topology/mass integrity) (`catalog:alluvial-engine-correctness-triage`)
- Stage extract + alluvial polish rename-split (`catalog:alluvial-top-pack-rename-split`)
- Agent Mermaid structure graph (`catalog:mermaid-structure-graph`)
- Dual-host partial: pure shell + stage web-in-process + CLI injector; host-shared Exact (`catalog:dual-host-shell-stage`)
- Architecture triage freeze phases 1 + 2-A (Exact placement, thin app extracts, session lifecycle) (`catalog:architecture-triage-freeze`)
- Analysis protocol P0–P2 envelope / interpretation honesty (`catalog:analysis-protocol-multi-host`)

## Now

- (no single active product Now after floor seed - re-aim from roadmap / user) (`none`)

## Next

- Browser folder ingest (user-selected directory feed) (`catalog:browser-folder-ingest`)
- Dual-host residual: VS Code `extension/` + webview loop (`catalog:dual-host-shell-stage`)
- Analysis protocol P3+ (Program CLI / browser worker / Tree-sitter breadth) (`catalog:analysis-protocol-multi-host`)

## Not now

- Interchangeable second lens / heatmap / DSM (`catalog:interchangeable-atlas-lenses`, `catalog:hierarchical-heatmap-lens`, `catalog:dependency-structure-matrix`)
- Full architectural time machine beyond impact CLI (`catalog:git-architectural-time-machine`)
- Carbon alluvial pan/scale rewrite (`catalog:carbon-alluvial-scale-and-pan`)
- Exact multi-language / Program re-index futures (`catalog:exact-surface-mode-futures`)

## Blocked

- (none)

<!--
Orientation SoR for this repo. Prefer ≤ one screen.
Each substantive bullet must include a catalog id (or explicit `none` for pure ops).
Do not auto-generate from catalog frontmatter. Do not put plan essays here.
Bodies stay in .grok/catalog/entries/*; ordered program index is .grok/reference/roadmap.md.
Update when: user asks / re-aim; ship closeout completes the Now item; campaign closeout tied to plan.
-->
